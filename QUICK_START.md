# 🚀 Быстрый старт

## ✅ Что уже сделано:

1. ✅ **База данных автоматически заполняется тестовыми данными** при первом запуске:
   - 8 докторов разных специальностей
   - 12 пациентов
   - 8 различных болезней

2. ✅ **Улучшен дизайн интерфейса:**
   - Современные градиенты
   - Красивые кнопки с анимациями
   - Улучшенные таблицы
   - Профессиональный вид

3. ✅ **Обновлен README.md:**
   - Добавлена секция с инструкциями по скриншотам
   - Список файлов для демонстрации кода
   - Инструкции по загрузке на GitHub

4. ✅ **Создан DbSeeder.cs:**
   - Автоматическое заполнение БД при запуске
   - Тестовые данные для демонстрации

## 🎯 Что нужно сделать:

### 1. Запустить проект

**Рекомендуемый способ: Запуск вручную (без Aspire)**

```bash
# Терминал 1: Backend
cd TestTask_API
dotnet restore
dotnet ef database update
dotnet run

# Терминал 2: Frontend
cd TestTask_Client
npm install
npm run dev
```

После запуска:
- API будет доступен по адресу: `https://localhost:5119` или `http://localhost:5119`
- Swagger UI: `https://localhost:5119/swagger`
- Frontend будет доступен по адресу: `http://localhost:5173`
- База данных автоматически заполнится тестовыми данными при первом запуске

**Альтернатива: Через Aspire (если DCP работает)**

Если хотите использовать Aspire Dashboard:
```bash
cd TestTask_AppHost
dotnet run
```

> **⚠️ Примечание:** Если Aspire не запускается из-за проблем с DCP, используйте ручной запуск выше.

### 2. Сделать скриншоты

Следуйте инструкциям в файле `SCREENSHOTS_GUIDE.md`

**Краткий список:**
- Интерфейс приложения (4 скриншота)
- Swagger UI (2 скриншота)
- Код (9 скриншотов)

### 3. Добавить скриншоты в README

После создания скриншотов, добавьте их в `README.md` в секцию "Скриншоты":

```markdown
![Patients List](screenshots/screenshot-01-patients-list.png)
```

### 4. Загрузить на GitHub

```bash
git add .
git commit -m "Add database seeder, improve UI, add screenshots guide"
git push origin main
```

## 📝 Важные файлы для скриншотов кода:

1. `TestTask_Domain/ValueObject/FullName.cs` - Value Object
2. `TestTask_Domain/Entites/Patient.cs` - Entity
3. `TestTask_Application/Services/PatientService.cs` - Service
4. `TestTask_Infrastructure/Repositories/PatientRepository.cs` - Repository
5. `TestTask_Infrastructure/Data/ApplicationDbContext.cs` - DbContext
6. `TestTask_API/Controllers/PatientController.cs` - Controller
7. `TestTask_API/Program.cs` - Configuration
8. `TestTask_Client/src/components/PatientList/PatientList.tsx` - React Component
9. `TestTask_Client/src/services/api.ts` - API Client

## 🎨 Советы:

- Используйте темную тему редактора для скриншотов кода
- Убедитесь, что код хорошо читается
- Обрежьте лишнее, оставьте только важное
- Используйте высокое разрешение (1920x1080+)

## 📸 Где разместить скриншоты:

Создайте папку `screenshots/` в корне проекта и поместите туда все скриншоты.

Удачи! 🚀

